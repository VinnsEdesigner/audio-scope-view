{
  "buildCommand": "pnpm run build",
  "outputDirectory": "apps/vyzorWeb/dist",
  "framework": "other",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://localhost:3000/api/:path*"
    }
  ]
}
