# audio-scope-view

## Frontend (web app) gotchas
- The home recordings list reads `useRecentRecordings` (query `GET_RECENT_RECORDINGS`), NOT `useRecordings` (`GET_RECORDINGS`). Any recording mutation (pin/rename/delete/create) must refetch `GET_RECENT_RECORDINGS` too, or the home list shows stale pin/rename/delete state. See `RECENT_RECORDINGS_REFETCH` in `apps/vyzorWeb/src/hooks/use-recordings.ts`.
- Main scroll container is `apps/vyzorWeb/src/root.tsx:55` (`overflow-y-auto`), which clips absolutely-positioned dropdowns that extend beyond it. `position: fixed` dialogs (session-settings, create-session) escape this, but their downward dropdowns can still run off the viewport on mobile — `InlineSelect` flips up / clamps `maxHeight` to available space to handle this.
- More-menu / popover z-index: use `z-50` (sticky header is `z-30`). The recordings more-menu previously used `z-10` and rendered behind later rows.
- GraphQL fragments/queries were verified to match the Rust schema via introspection (SESSION_FIELDS, RECORDING_FIELDS, RECORDING_SUMMARY_FIELDS, SessionWithStatusFields, all wrapper result types).

## Docker / Client-Server Connection (IMPORTANT)
- The deployed image bundles a static-server (Node, port 3000) that serves the SPA and reverse-proxies `/graphql` and `/ws` to the Rust backend (127.0.0.1:8080 inside the container). So the browser must use **relative** endpoints (`/graphql`, `/ws`), NOT absolute `ws://localhost:8080/...` URLs — the browser's `localhost` is not the container's localhost, so absolute URLs break in Docker/remote deploys.
- `packages/api-client/src/config.ts` `DEFAULT_CONFIG`: `graphqlEndpoint: "/graphql"`, `websocketEndpoint: "/ws"`. `apps/vyzorWeb/src/hooks/use-waveform-stream.ts` builds the WS URL from `location.host` + `/ws`. `.env.example` and `render.yaml` set `VITE_WEBSOCKET_ENDPOINT=/ws`.
- `VITE_*` env vars are baked into the frontend bundle at **build** time (Vite), not read at runtime. A deployed image with stale/empty build-time env will be broken regardless of runtime env — rebuild the image to pick up new values.
- **Image freshness matters:** the ghcr image must be rebuilt from current source after client-side changes. A stale image renders the UI but the Apollo client may silently fail to fire queries (no POST /graphql in server logs). Always rebuild + redeploy after frontend fixes.
- Run locally: `docker run -p 3000:3000 -p 8080:8080 -e BOOTSTRAP_KEY="$(openssl rand -hex 32)" ghcr.io/vinnsedesigner/audio-scope-view:latest`. Health: `curl http://localhost:3000/health`.
- Dev frontend (no Docker): `pnpm --filter @audio-scope-view/vyzor-web dev` (add a `server.proxy` in vite.config.ts pointing `/graphql`+`/ws` at the backend port to test against a running server).

## C++ DSP Core (sdk/) — Step 1 of the ARCHITECTURE migration, implemented
- The single DSP source of truth. Compiled to 5 targets (Linux/Windows/Android/WASM/Rust FFI) via CMake presets in `sdk/CMakePresets.json`.
- Build + test (Linux host): `cd sdk && cmake --preset linux && cmake --build --preset linux && ctest --preset linux`. Requires `cmake`, `ninja`, `pkg-config`, `libasound2-dev`, `liblz4-dev`, `libgtest-dev` (or googletest fetched via FetchContent).
- 36 GoogleTest cases across 7 suites, all ported from the Rust `#[test]` vectors in `rust/src/domain/*` and the live TS `scope-dsp.ts`. Run: `ctest --preset linux`.
- Algorithms ported faithfully (NOT rewritten): FFT radix-2 from TS `scope-dsp.ts` (the live one); spectrum struct + windowing from Rust `fft_processor.rs`; measurements/THD/SNR/harmonics from Rust `measurements.rs`; spectrogram STFT from Rust `spectrogram.rs`; trigger from the **live TS** `findTriggerIndex` (the Rust `trigger/detector.rs` is dead code — see ARCHITECTURE_IMPLEMENTATION_SPEC §6.4); LZ4 compression from Rust `compression/mod.rs` via the C `lz4.h` API directly; generators (sine/square/saw/triangle/noise) from Rust `waveform_generators.rs` with a deterministic xoshiro PRNG (no system RNG dependency, reproducible tests).
- `corrections.cpp` inverse-frequency-response needs the IFFT path (forward-only FFT today) — stub marked for Step 2. DC offset + normalize_peak + noise-gate interpolation + AGC estimate are complete.
- LZ4 on small raw-float buffers can EXPAND (negative compression ratio is expected, not a bug) — the test asserts round-trip losslessness, not ratio positivity.
- Static libs: `sdk/build/linux/dsp/libaudioscope_dsp.a`, `sdk/build/linux/common/libaudioscope_common.a`.

## Build & Run
- Rust project in `rust/` directory; pinned toolchain `nightly-2026-07-20` (via `rust-toolchain.toml`)
- Build: `cd rust && cargo build --release` (release takes ~2m; clean — no warnings)
- **Native link deps (host):** the C++ DSP core is compiled by `build.rs` via the `cc` crate and
  links `-lstdc++`, `-llz4`, and (via cpal) `-lasound`. A fresh host needs
  `apt-get install libasound2-dev liblz4-dev libxxhash-dev pkg-config` (plus a C++17 compiler)
  or `cargo build` fails at link with `unable to find library -lasound/-llz4`. The runtime
  `.so` packages alone are NOT enough — the `-dev` packages provide the `.so` symlinks the
  linker needs.
- **CI gates (must stay green):** `cargo fmt --all -- --check`,
  `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --all-features --all-targets`.
  `cargo clippy --fix` (then `cargo fmt`) handles most mechanical lints; `clippy::result_large_err`
  is fixed by boxing the large `Err` variant (`Result<T, Box<BigErr>>`).
- Run against Turso cloud:
  ```
  cd rust
  export APP__DATABASE__URL="$TURSO_DB_URL"          # libsql://vyzor-scopedb-vinnsedesigner.aws-us-west-2.turso.io
  export TURSO_VYZOR_SCOPE_DB_TOKEN="$TURSO_VYZOR_SCOPE_DB_TOKEN"  # DB-scoped token (read directly by server)
  export BOOTSTRAP_KEY="$(openssl rand -base64 32)"  # >=16 chars, required
  export APP__SERVER__HOST="0.0.0.0" APP__SERVER__PORT="8090"
  ./target/release/audio-scope-view
  ```
- Health check: `curl http://127.0.0.1:8090/health` -> "Yes am alive"
- GraphQL endpoint: `POST http://127.0.0.1:8090/graphql` with `Authorization: Bearer <bootstrap-key>`
- No-auth request returns 200 with GraphQL error "Unauthorized..." (auth enforced at GraphQL layer)

### Turso Token Distinction (IMPORTANT)
- `TURSO_AUTH_TOKEN` (secret) = **platform API token** → works for `turso` CLI (`TURSO_API_TOKEN=$TURSO_AUTH_TOKEN turso db list`), returns **401** against the DB HTTP API. NOT read by the server.
- `TURSO_VYZOR_SCOPE_DB_TOKEN` (secret) = **DB-scoped token** → returns **200** against the DB HTTP API. The server reads this env var directly in `database_connection.rs::new_turso()`.
- `TURSO_DB_URL` (secret) = `libsql://vyzor-scopedb-vinnsedesigner.aws-us-west-2.turso.io`
- Turso CLI binary: `~/.turso/turso` (add `$HOME/.turso` to PATH); installed via `curl -sSfL https://get.tur.so/install.sh | sh`

## Turso HTTP API Integration

### Architecture
- `infrastructure/turso_http_client.rs`: TursoClient uses Turso v2 pipeline API (`POST /v2/pipeline`)
- All repos (`repo_turso_*.rs`) use parameterized queries (`?` placeholders) for SQL injection safety
- `TursoArg` enum serializes bound parameters with `{"type":"text/integer/float/null","value":...}` tags

### Key Gotchas (Fixed)
1. **NaN/Infinity serialization**: serde_json serializes `f32::NEG_INFINITY`/`NaN` as `null`, which Turso rejects for NOT NULL columns. `TursoArg::float()` sanitizes non-finite values to 0.0.
2. **SELECT * column ordering**: `ALTER TABLE ADD COLUMN` appends columns to the end, breaking positional index assumptions in `row_to_*` mappers. Use explicit column lists (e.g. `RECORDING_COLUMNS` constant in recording repo, explicit SELECT in user_preferences repo) instead of `SELECT *`. Affected: recordings (sample_rate added by migration 008), user_preferences (auto_close_timeout_secs).
3. **Multi-statement SQL**: Turso v2 API rejects multi-statement SQL strings. `database_connection.rs` splits them before sending.
4. **TursoResult enum**: Match on `TursoResult::Ok(ok)` to extract results, `TursoResult::Error { error }` for errors.

### Turso DB
- Cloud DB: `libsql://vyzor-scopedb-vinnsedesigner.aws-us-west-2.turso.io` (location: aws-us-west-2)
- DB-scoped token: `$TURSO_VYZOR_SCOPE_DB_TOKEN` secret (read directly by the server)
- Platform token: `$TURSO_AUTH_TOKEN` secret (use as `TURSO_API_TOKEN` for `turso` CLI management)
- Tables: `_migrations`, `api_keys`, `recordings`, `sessions`, `settings`, `user_preferences`, `waveforms`
- Verified end-to-end (2026-08-08): migrations apply, CRUD via GraphQL (createSession/createSettings/endSession/pinRecording), reads (sessions/dashboardSummary/recentRecordings/settings), REST `/api/recordings/<id>/metadata` — all persist to cloud DB.

## GraphQL Schema Notes
- Recordings: `createRecording`, `recording(id)`, `recordings(filter)`, `recentRecordings`, `renameRecording`, `pinRecording`, `deleteRecording`, `deleteRecordings`
- Sessions: `createSession`, `session(id)`, `sessions`, `updateSessionDsp(id, input)`, `endSession`
- Settings: `settings(sessionId)`, `updateSettings`
- Use `input` wrapper for mutation arguments (e.g. `updateSessionDsp(id, input: {...})`)
- Static content (no device scoping): `aboutInfo`, `features`, `changelog` — served from JSON embedded via `include_str!("../../data/*.json")` at compile time, so it works in Docker containers without the `data/` dir on disk.

## Device-ID Scoping (IMPORTANT)
- **No user-facing auth.** Sessions/preferences are scoped to a device via the `X-Device-Id` HTTP header (no sign-up/login). A larger platform will wrap this app as a feature with its own auth later.
- Frontend: `apps/vyzorWeb/src/hooks/use-device-id.ts` (`useDeviceId`) generates/persists a UUID per browser (localStorage). The api-client attaches it as `X-Device-Id` on every request and as a query param on the WS connection.
- Backend: `extract_auth_header` middleware reads `Authorization` + `X-Device-Id` and inserts them as `AuthHeaderExt(Option<String>)` / `DeviceIdExt(Option<String>)` newtype wrappers into the request extensions. **These must be distinct types** — axum's `Extension` map is keyed by type, so two `Option<String>` inserts collide and the second overwrites the first (previously caused the device-id value to clobber the auth-header value, breaking all auth).
- `RequestIdentity` (built in `graphql_handler`) carries `device_id` + `is_system_client` + `api_key`; resolvers read it via `ctx.data::<RequestIdentity>()` and use `device_scope_from_context(ctx)` to scope queries/mutations.
- Ownership checks: batch recording ops (`deleteRecording`, `deleteRecordings`, `pinRecordings`) verify each item belongs to the device. Subscription resolvers (`waveform_subscribe`, `spectrum_subscribe`, etc.) verify session ownership before streaming. `endSession`/`updateSessionDsp` only affect sessions owned by the requesting device.
- **WebSocket auth + scoping:** The WS handshake (`/ws`) is behind the same `extract_auth_header` middleware. Because browsers cannot set custom headers on a WebSocket handshake, the frontend also sends `X-Device-Id` and `X-Api-Key` as query params (`WsHandshakeQuery`), which the `ws_handler` merges with any header values. The handler authenticates the bootstrap/API key (401 on failure), builds a `RequestIdentity`, and passes it + the `SessionService` into `handle_socket`. Every `Subscribe`/`SubscribeSpectrum`/`WaveformData`/`AnalysisData` message is checked by `verify_ws_session_ownership` against `session.user_id == device_id` before acting — so a device cannot subscribe to or publish into another device's session.
- User preferences: `prefs_id` = device id (falls back to `"default"` for system/admin calls with no device id).
- Verified end-to-end: device A's sessions/preferences are invisible to device B; unauthenticated requests are rejected.

## GraphQL Subscriptions over WebSocket (Apollo `graphql-ws` protocol)
- The app exposes TWO WebSocket endpoints — do not confuse them:
  - `/ws` — legacy binary streaming protocol (waveform/spectrum frames, `Subscribe`/`WaveformData` JSON messages). Used by `use-waveform-stream.ts`.
  - `/graphql/ws` — Apollo `graphql-transport-ws` subprotocol for `@apollo/client` subscriptions (`analysisSubscribe`, etc.). Used by `use-scope-capture.ts` via the Apollo `WebSocketLink`.
- Backend (`rust/src/api/server_graphql.rs`): `graphql_ws_handler` serves `/graphql/ws`. The `WebSocketUpgrade` MUST call `.protocols(["graphql-transport-ws"])` so axum echoes the subprotocol back — without it the browser's `WebSocket` never fires `open` (subprotocol negotiation fails). `GraphqlWsHandshakeQuery` reads `X-Device-Id` / `X-Api-Key` from serde-renamed query params and authenticates the same way as `/ws`.
- Frontend (`packages/api-client/src/audioScopeView/graphql/client.ts`): the Apollo `WebSocketLink` connects to `ws(s)://<host>/graphql/ws?X-Device-Id=<deviceId>` (device id appended as a query param because browsers cannot set custom headers on a WS handshake). `@apollo/client/link/ws` depends on `subscriptions-transport-ws` as a peer dep — install it (`pnpm --filter @audio-scope-view/vyzor-web add subscriptions-transport-ws`) or the import resolves to `undefined` and subscriptions silently never connect.
- Static-server proxy (`apps/vyzorWeb/scripts/static-server.cjs`): the WS upgrade handler proxies `/graphql/ws` (and `/ws`) to the backend. CRITICAL: the `http.request`/`http.upgrade` options MUST include `port: 8080` — without it Node defaults the target port to 80 and the upgrade fails silently. Also inject `X-Device-Id` / `Authorization` headers into the proxied WS handshake request when the browser omits them.
- Verification: a working subscription shows `GQL-WS: AUTH OK (device: <id>)` in the backend log and NO `ws close`/`error` lines. Direct test against 8080 and through the 3003 proxy should both reach `connection_ack`.

## Apollo Error Toast Sanitization
- `apps/vyzorWeb/src/lib/format-error.ts` `formatError(error)` is the single entry point for all user-facing toasts. It strips `http(s)://` and `ws(s)://` URLs from messages, collapses whitespace, and maps `Failed to fetch` / `NetworkError` / `Load failed` to `"Network error: unable to reach the server. Check your connection and try again."` so internal endpoints (`http://127.0.0.1:8080/graphql`) never leak to the UI.
- `packages/api-client/src/audioScopeView/graphql/client.ts` `sanitizeErrorMessage` does the same for Apollo `onError` link messages.

## DB Backend Selection (recap — got bitten by this)
- `DatabaseConnection::new(config.database.url)` picks Turso iff the URL starts with `libsql://`, else local SQLite. Config default is `sqlite:./data/audio_scope_view.db`.
- Restarting the backend WITHOUT `APP__DATABASE__URL=$TURSO_DB_URL` silently falls back to a **fresh local SQLite** DB (0 recordings) even though `TURSO_*` env vars are set — the env vars alone do NOT select Turso; only the `libsql://` URL does. Always launch with `APP__DATABASE__URL="$TURSO_DB_URL"`.
- The local SQLite file location depends on the process CWD (`./data/audio_scope_view.db` relative to wherever the binary is launched), so `rust/data/` vs repo-root `data/` can both exist and diverge.

## Device-scoped data access (security fix)
- The `X-Device-Id` header is the data-isolation dimension. `is_valid_device_id()` in `rust/src/shared/constants.rs` validates canonical UUID form (`8-4-4-4-12` hex) and the legacy `dev-<base36>-<base36>` fallback. Malformed ids are rejected with HTTP 400 at the middleware (`server_graphql.rs::extract_auth_header`) and both WS handshake handlers (`ws_handler`, `graphql_ws_handler`).
- Device scoping is enforced at the **resolver** layer via ownership helpers (`assert_session_ownership`, `assert_settings_session_owned`, `assert_waveform_session_owned`): before reading/mutating a session-scoped resource, verify the session's `user_id` equals the requesting device id. Unscoped admins (bootstrap key, no device id) bypass the check. Denials return "not found" rather than "forbidden" to avoid leaking existence.
- Settings CRUD, Waveform CRUD, `capture`, `updateSessionDsp`, `createSubSession`, `subSessions`, `parentSession`, `exportWaveform`, and `startBatchCapture` all enforce ownership. API key management (`apiKeys`, `apiKey`, `verifyApiKey`, `createApiKey`, `updateApiKey`, `deleteApiKey`) is gated behind `assert_admin` (bootstrap-only).
- `static-server.cjs` explicitly forwards `X-Device-Id` (not just via `...req.headers` spread) in the `/graphql`, `/api/`, and WS upgrade proxy paths.
- `createSession` derives `user_id` from the request device id (ignores client-supplied `input.user_id` for non-admins).

## Live audio backend (mocks/stubs removed — cpal is the only backend)
- The Rust backend now has a **single** audio backend: cpal (live capture from a physical or virtual input device). The `mock-audio`, `pulse`, and `real-audio` cargo features and the `audio_capture_mock.rs` / `audio_capture_pulse.rs` / `audio_capture_alsa.rs` stub files have been **deleted**. `cpal` is now a non-optional dependency (`cpal = "0.15"` in `Cargo.toml`); build with a plain `cargo build --release` (no `--features` flag).
- `AudioBackendType` is a single-variant enum (`Real` only), kept for the `AudioStreamManager::backend_type()` logging API. `AudioConfig` in `config_loader.rs` is now an empty struct whose `backend_type()` always returns `Real`; the `[audio] backend = "..."` config key / `APP__AUDIO__BACKEND` env var are **ignored** (there is no longer a choice to make). The `[audio]` section was removed from `config.toml`.
- `AudioStreamManager` always uses `RealAudioCapture`: `new()` / `with_backend(_)` (arg ignored, kept for compat) / `init_capture()` / `capture_once(duration_ms)` / `list_devices()` all instantiate cpal directly. The `MockAudioCapture` and `PulseAudioCapture` `AudioCaptureBackend` impls were deleted; only the `RealAudioCapture` impl remains.
- The `capture` GraphQL mutation calls `context.audio_manager.capture_once(duration_ms)` (the `AudioStreamManager` is injected into `GraphqlContext` via `AppState::new`); the old hardcoded `MockAudioCapture` sine-wave path is gone. `CaptureSettingsInput.frequency`/`amplitude`/`noise_level` are accepted but ignored by the live backend; only `duration_ms` matters.
- **Build deps:** cpal needs `pkg-config` + `libasound2-dev` (ALSA headers) at compile time. cpal probes ALSA devices at runtime via the `libasound2` ALSA plugins.
- **Runtime audio source in this sandbox:** no `/dev/snd` hardware and `modprobe snd-aloop` fails (no CAP_SYS_ADMIN). The only viable source is a **PulseAudio system daemon** with its null-sink monitor (`auto_null.monitor`). Start it system-wide (non-root `--system` is refused; must run as root/sudo): set up an auth cookie at `/var/run/pulse/.pulse-cookie` (and `/var/run/pulse/.config/pulse/cookie`), then `PULSE_RUNTIME_PATH=/var/run/pulse pulseaudio --system --disallow-exit --realtime=false`. Copy the cookie to `~/.config/pulse/cookie` + `~/.pulse-cookie` for the non-root backend process and export `PULSE_SERVER=/var/run/pulse/native` + `PULSE_COOKIE=~/.pulse-cookie`.
- **Harmless noise:** cpal/ALSA emits many `ALSA lib confmisc.c: Cannot get card index for 0`, `Cannot open device /dev/dsp`, and JACK `Cannot connect to server socket` warnings while probing — these are expected in a headless container and do not prevent the PulseAudio `pulse`/`default` devices (2 ch, 44100 Hz) from being found and opened.
- **Run command:** `BOOTSTRAP_KEY=... APP__DATABASE__URL="$TURSO_DB_URL" APP__DATABASE__TOKEN="$TURSO_VYZOR_SCOPE_DB_TOKEN" PULSE_SERVER=/var/run/pulse/native PULSE_COOKIE=~/.pulse-cookie RUST_LOG=info ./target/release/audio-scope-view` (no `APP__AUDIO__BACKEND` needed; no `--features` flag). Verified: `capture` returns 6615 samples @ 44100 Hz for a 150 ms request via the cpal backend.
- **Known pre-existing test failure (unrelated):** `audio_capture_real::tests::test_ring_buffer_wrap` fails (`push` returns 7, test expects 9) — this is a bug in the `AudioRingBuffer` push/drain logic in `audio_capture_real.rs`, NOT related to the mock removal (that file was not touched).

## DSP core: C++ → WASM → TS (architecture migration, Step 3 complete)
- **Single source of truth:** the DSP core is C++ in `sdk/dsp/` (`audioscope::dsp` namespace). The Rust FFI (`rust/src/infrastructure/dsp_ffi.rs`) and the WASM/TS wrapper (`packages/dsp-wasm/`) both call the SAME flat C ABI in `sdk/bindings/ffi/audioscope_ffi.{h,cpp}`. There is no duplicate TS FFT anymore — `packages/dsp-wasm/src/audioscope-dsp.ts` is a thin memory-marshalling wrapper, not a reimplementation.
- **C ABI (the FFI seam):** `audioscope_ffi.h` declares `as_*` C structs + `as_*` free functions. Composite returns (spectrum, harmonics, spectrogram, waveform analysis, compressed waveform) are returned as **malloc'd struct pointers** with inner arrays; callers read fields at fixed offsets then call the matching `as_*_free` (frees inner arrays) + `em_free`/`free` (frees the holder). Array returns (magnitudes, triggered window, generated waveform, resampled) use the **malloc'd pointer + `int* out_len`** pattern — robust across all binding runtimes.
- **WASM build (`sdk/wasm/`):** `emscripten_main.cpp` has `EMSCRIPTEN_KEEPALIVE` `extern "C"` wrappers (`em_*`) over the C ABI. CRITICAL: must be `extern "C"` or symbols get C++-mangled and the JS `EXPORTED_FUNCTIONS` list (by C name) fails to link. `CMakeLists.txt` links the `audioscope_dsp`/`audioscope_common` static libs with `--whole-archive` (KEEPALIVE exports have no C++ callers and would be stripped otherwise). emcc flags: `-O3 -msimd128 -msse -msse2 MODULARIZE=1 EXPORT_ES6=1 EXPORT_NAME=AudioScopeDspModule ALLOW_MEMORY_GROWTH=1`. `build.sh` runs emcmake/emmake and stages `audioscope.{js,wasm}` to `packages/dsp-wasm/dist/`.
- **WASM i64 marshalling gotcha:** Emscripten `ccall`/`cwrap` infers arg types from the C signature; `long long`/`int64_t` args require BigInt at the JS call site (`Cannot convert 0 to a BigInt`). The `em_*` wrappers use **i32** for `holdoff` (samples) and `start_time_ms` (fits in i32: ~24 days) and cast to i64 internally — the underlying `as_*` ABI keeps int64_t. Do not reintroduce `long long` in `em_*` signatures.
- **EXPORTED_FUNCTIONS vs EXPORTED_RUNTIME_METHODS:** `_malloc`/`_free` belong in `EXPORTED_FUNCTIONS` (compiled exports), NOT `EXPORTED_RUNTIME_METHODS` (JS-side helpers like `ccall`/`getValue`/`HEAPF32`). Putting them in the latter causes "undefined exported symbol: _malloc".
- **Vendored LZ4:** `sdk/dsp/third_party/lz4/` (LZ4 1.10.0 sources) makes portable targets self-contained. `compression.cpp` only uses `LZ4_compress_default`, `LZ4_compress`, `LZ4_decompress_safe`. The `audioscope_lz4_vendored` CMake target compiles only `lz4.c`. `AUDIOSCOPE_USE_VENDORED_LZ4=ON` (set in the `wasm` CMake preset) selects it; native builds use system lz4.
- **`packages/dsp-wasm/` package:** `AudioScopeDsp` class is the browser/worklet analog of the Rust `dsp_ffi.rs`. Memory model: alloc f32 into HEAPF32 → call `em_*` → copy result out via `HEAPF32.slice` → free. Struct field offsets are hardcoded constants (wasm32, natural alignment) in `audioscope-dsp.ts` matching `audioscope_ffi.h`. Two scratch pointers (`tmpLenPtr`, `tmpMagPtr`) are reused across calls to avoid per-call malloc churn. The generated `audioscope.js`/`.wasm` are gitignored (regenerated by `build:wasm`); the hand-written `dist/audioscope.d.ts` is force-added so typecheck works in a fresh checkout.
- **Build config:** root `package.json` has `build:wasm` (= `bash sdk/wasm/build.sh`), `build:sdk`, `test:sdk` (ctest), `test:wasm` (vitest). `build` depends on `build:wasm`. `turbo.json` declares `build:wasm`/`build:sdk`/`test:sdk`/`test` tasks (the vitest `test` task depends on `build:wasm`). The stale `dev:web`/`dev:mobile` scripts were removed.
- **Testing parity across all three bindings:** native C++ SDK tests (ctest, 36 tests in `sdk/`), WASM vitest (`packages/dsp-wasm/test/dsp-wasm.test.ts`, 12 tests — loads WASM in Node, FFT on a 440 Hz sine, asserts peak; covers measurements, harmonics, LZ4 round-trip, trigger, generators), and Rust FFI tests (`cargo test`, 31 tests in `rust/`). All three exercise the same C++ core, so parity is provable.
- **Toolchain versions (sandbox):** cmake 3.31.6, ninja 1.12.1, emsdk 6.0.6 (emcc), node v22.23.2, pnpm 11.15.1. emsdk at `/home/openhands/emsdk` (run `source $HOME/emsdk/emsdk_env.sh` before any `cmake --preset wasm`/emcc command).
- **Branch:** `feat/cpp-dsp-core-rust-ffi-migration`. Step 1+2 = commit `437b7f7` (C++ core + Rust FFI bridge + Rust DSP files deleted). Step 3 = commit `2694101` (WASM build + dsp-wasm TS package + root build config). Remote: `github.com/VinnsEdesigner/audio-scope-view`.

## Web app wiring: dsp-wasm + WebGL2 renderer (architecture migration, Steps 4–5 complete)
- **Single DSP source of truth in the browser:** the web app no longer has a duplicate TS FFT. `apps/vyzorWeb/src/lib/scope-dsp.ts` (radix-2 FFT, `computeSpectrum`, `toDecibels`, `triggeredWindow`, `findTriggerIndex`, `resampleTo`) is **deleted**. All DSP flows through `@audio-scope-view/dsp-wasm` (the C++ core compiled to WASM). The web app's `AudioScopeDsp` calls are the browser/worklet analog of the Rust `dsp_ffi.rs` — same C ABI, same core.
- **Singleton loader (`apps/vyzorWeb/src/lib/dsp-loader.ts`):** `ensureDsp()` (async, kicks off + awaits the WASM load, memoized, failure-tolerant → returns null) and `getDsp()` (sync, returns the loaded instance or null). Shared by `scope-canvas.tsx`, `use-audio-analyzer.ts`, `use-waveform-generator.ts`, `measurements-dialog.tsx`. The WASM module is expensive to instantiate (fetch + compile); it must load exactly once. Hot-path callers (rAF draw loop, capture tick) read `getDsp()` and degrade gracefully (free-run / inline fallback) until loaded — the UI never blocks on module load.
- **Consumers wired:** (1) `scope-canvas.tsx` — spectrum is drawn from a precomputed WASM `Spectrum` (dB magnitudes + frequencies, no `toDecibels` step); `triggeredWindow` calls `dsp.triggeredWindow` (returns `Float32Array | null`); `ensureDsp()` on mount. (2) `use-audio-analyzer.ts` — RMS/peak/frequency via `dsp.computeRms`/`findPeakAmplitude`/`estimateDominantFrequency` (with tiny inline TS fallbacks pre-load). (3) `measurements-dialog.tsx` — Vpp/RMS/DC-offset/frequency via the WASM core. (4) `use-waveform-generator.ts` — the old TS `generateSample` synthesizer in `use-mock-audio-analyzer.ts` is replaced by `dsp.generateWaveform` (C++ sine/square/saw/triangle/noise generators); the generator hook drives the new `WaveformGeneratorDialog` (Activity icon in the top bar), which replaced the non-functioning "test mode".
- **WASM asset serving:** `vite.config.ts` adds `optimizeDeps.include: ["@audio-scope-view/dsp-wasm"]` and `assetsInclude: ["**/*.wasm"]`; the production build emits `dist/client/assets/audioscope-*.wasm` (42K) as a referenced asset. `scripts/static-server.cjs` adds `.wasm → application/wasm` to its MIME map (otherwise the browser refuses to compile/stream the module).
- **Type compatibility:** the ui-store's `triggerEdge: "rising" | "falling" | "auto"` is structurally identical to the WASM `TriggerEdge`, so no adapter is needed. `frame` in `scope-canvas.tsx` is `ArrayLike<number>` (the WASM `triggeredWindow` returns `Float32Array`; held frames are `number[]`), so the `for...of` loop was converted to an index loop (`ArrayLike` is not `Iterable` in strict TS).
- **Root `test:sdk` script bug (fixed):** the script did `cd sdk/build/linux && ctest --preset linux`, but `CTestPresets.json` lives in `sdk/`, so ctest could not find the presets from the build subdir. Fixed to `(cd sdk && ctest --preset linux)`.
- **WebGL2 renderer (`src/lib/webgl/`, Step 5 / spec §C.1 — DONE):** the Canvas2D path in `scope-canvas.tsx` is **fully removed** (no `getContext("2d")` remains). Raw WebGL2 is used directly — **no `regl`/`gl-matrix` deps were added** (the spec offered "regl or raw WebGL2 — decide"; raw was chosen, with a hand-rolled `mat4.ts` for ortho/identity). Renderers (all under `src/lib/webgl/`, barrel `index.ts`): `ScopeRenderer` (instanced line trace), `SpectrumRenderer` (spectrum bars), `SpectrogramRenderer` (2D waterfall texture), plus two the spec didn't enumerate — `GlyphRenderer` (SDF text for labels/readouts) and `OverlayRenderer` (grid/cursors/markers). Shaders: `shaders/{line,spectrum,spectrogram,glyph}.{vert,frag}` + `shaders/index.ts`. `GLContext.from(canvas)` acquires one WebGL2 context per canvas and owns the program cache; `scope-canvas.tsx` instantiates a `RendererBundle` of all five renderers sharing it. View selection: `ui-store.ts` `ScopeView = "time" | "spectrum" | "spectrogram"` (spectrogram is new in this step).
- **WebGL2 fallback decision (spec hazard #11 — resolved as: no Canvas2D fallback):** `GLContext.from()` returns `null` when WebGL2 is unavailable; `scope-canvas.tsx` then `console.warn`s and renders an empty surface (no Canvas2D fallback path). WebGL2 is a hard runtime requirement (Chrome/Edge/Firefox/Safari post-2020 all provide it). Do not reintroduce a Canvas2D path.
- **Waveform Generator (replaces "test mode"):** `test-mode-icon.tsx` + the old test-mode UI are deleted; `WaveformGeneratorDialog` (`components/scope/waveform-generator-dialog.tsx`) + `useWaveformGenerator` (`hooks/use-waveform-generator.ts`) feed the scope from the C++ DSP core via WASM (`dsp.generateWaveform`), so rendering/measurements can be verified without a live audio input. Open the dialog (Activity icon), pick Sine/Square/Sawtooth/Triangle/Noise (+ noise color), and the measurement panel shows expected Vpp/Freq/Win.
- **§C.3 playback worklet repointed to the C++ core (DONE):** `apps/vyzorWeb/src/audio/worklets/audio-streaming-processor.js` no longer just plays PCM chunks — it loads the same WASM `AudioScopeDsp` the live-capture path uses and runs per-block DSP (`_em_compute_magnitudes` + `_em_analyze_waveform`) on the played samples, posting `{type:"frame", magnitudes, analysis, sampleRate}` to the main thread for the WebGL renderer. This unifies the DSP source of truth across live capture and playback. The worklet loads the WASM module by **URL** (the main thread resolves the Vite-hashed `audioscope.js`/`.wasm` URLs via `?url` imports in `use-streaming-playback.ts` and passes them in the `config` message); the worklet can't use a bundler `import` (AudioWorklet global scope, flat public URL). `locateFile` is overridden to point at the binary URL (the default `import.meta.url`-based lookup would resolve relative to the worklet — wrong). DSP load is failure-tolerant: if the module fails to load, audio still plays — only the per-block frames stop (mirrors `dsp-loader`'s degradation). `use-streaming-playback.ts` gained an `onDspFrame` option; the `frame`/`dsp_ready`/`dsp_error` messages are typed in the worklet `.d.ts`. The renderer consumes playback frames with the same shape as live frames.
- **`sdk/common/` completion (spec §A.2 — DONE):** the "future/to be done" comments are gone. `buffer.hpp` now has `PoolAllocator<T>` — a free-list object pool with **stable-address** backing storage (`std::vector<std::unique_ptr<T>>`, so `emplace_back` never invalidates an outstanding pointer) and a lock-free Treiber-stack free list; once warm, `acquire`/`release` touch no heap. `config.hpp`/`config.cpp` now have `from_json`/`to_json` (a tiny dependency-free hand-rolled JSON object parser — `common` stays Layer-1-clean, no nlohmann) + `DspConfig::normalized()` (clamps overlap to [0,1), rounds `fft_size` to next pow2, bounds the spectrogram band to Nyquist, swaps inverted bands) + `parse_window_type`/`window_type_name`. `types.hpp`/new `types.cpp` add `sample_format_size()` + `convert_samples_to_f32()` (S16/S32/F32 → normalized F32, the single normalization path every platform binding calls). **Bug fix while here:** the pre-existing `AudioRingBuffer` had a latent correctness bug — `(w - r) % capacity_` made a full ring indistinguishable from an empty one (so `push` overwrote past capacity and `available()` returned 0 when full). Fixed to use `w - r` (monotonic indices, never wrapped) as the count directly; the modulo is only for the physical slot index. New `sdk/tests/test_common.cpp` (25 tests: ring buffer round-trip/wrap/concurrency, pool reuse/growth/stable-address, JSON parse/round-trip/unknown-keys/malformed, normalization clamps, S16/S32/F32 conversion). Suite: **61/61 ctest green** (36 original + 25 new common).
- **All suites green (final):** 12 WASM vitest, 61 native C++ SDK (ctest), vyzorWeb tsc clean, dsp-wasm tsc clean. Rust suite (31 tests) is unaffected by the C++/TS work (its `AudioRingBuffer` in `audio_capture_real.rs` is an independent Rust-native struct, not a consumer of `audioscope_common`; the FFI calls only the unchanged `_em_*` C ABI). Cargo is not installed in this sandbox; run `cargo test --workspace` where the Rust toolchain is present.
- **Branch:** `feat/cpp-dsp-core-rust-ffi-migration`. Step 4 = commit `5f7ab61` (wire dsp-wasm into web app + delete `scope-dsp.ts`). Step 5 = commit `3cd0989` (WebGL2 renderer + spectrogram view + waveform generator; deletes `use-mock-audio-analyzer.ts`). §C.3 + sdk/common completion = this changeset.

## Mobile eject + JSI (spec §D — `apps/vyzorMobile/`, STEP 5 DONE)
- **Stack:** bare/prebuilt RN 0.76.3 New Architecture app, Expo ~52 (managed autolinking, no Expo Go), Expo Router ~4, NativeWind 4.1, zustand, @tanstack/react-query, @react-native-async-storage/async-storage. UI reuses `packages/ui-radix/` (Radix + Tailwind) — **no Tamagui**.
- **JSI bridge (TurboModule via classic ReactPackage):** Kotlin `DspModule` (`android/app/src/main/java/com/audioscope/dsp/DspModule.kt`, `getName()="AudioScopeDsp"`, extends `ReactContextBaseJavaModule`, `@ReactMethod` wrappers + `private external fun native*`) loads `System.loadLibrary("audioscope_dsp")`. `DspPackage.kt` registers it; `MainApplication.kt getPackages()` adds `DspPackage()` to the `PackageList`. JS side (`app/lib/dsp.ts`) reads `NativeModules.AudioScopeDsp`. C++ side: `sdk/bindings/android/jni_bridge.cpp` (12 `Java_com_audioscope_dsp_DspModule_native*` JNI exports + `JNI_OnLoad`) + `oboe_capture.cpp` (`OboeCapture : AudioBinding`, AAudio/OpenSL ES capture, int16/int32→f32 normalization via the shared `convert_samples_to_f32`). CMake builds the DSP core from source (cross-compiled by NDK) into `libaudioscope_dsp.so`.
- **Build verified:** `./gradlew assembleDebug` → BUILD SUCCESSFUL. APK at `apps/vyzorMobile/android/app/build/outputs/apk/debug/app-debug.apk` (~144M debug). `libaudioscope_dsp.so` packaged for all 4 ABIs (arm64-v8a, armeabi-v7a, x86, x86_64). `llvm-nm -D` confirms `JNI_OnLoad` + all 12 JNI symbols exported. `tsc --noEmit` clean (exit 0).
- **Toolchain (sandbox):** NDK 26.1.10909125 (default) + 25.2.9519653, cmake 3.22.1 (Android SDK-bundled), Java 21 (`/usr/lib/jvm/java-21-openjdk-amd64`), `ANDROID_HOME=$HOME/android-sdk`. **Now actually installed in this sandbox** (`$HOME/android-sdk`, 2.6 GB): cmdline-tools 13.0 (sdkmanager 19.0), platform-tools 37.0.1, platforms;android-35, build-tools;35.0.0, ndk;26.1.10909125, cmake;3.22.1. Env vars (`ANDROID_HOME`/`ANDROID_SDK_ROOT`/`JAVA_HOME`/`PATH` including `cmdline-tools/latest/bin`, `platform-tools`, the NDK dir, and `$JAVA_HOME/bin`) are set in **`~/.profile`** (login shells) AND `~/.bashrc` (interactive shells) — note `.bashrc` early-returns for non-interactive shells, so `.profile` is the canonical source for login-shell env. Verified: `bash -lc 'which ndk-build adb sdkmanager'` resolves all three. Build env: `export ANDROID_HOME=$HOME/android-sdk ANDROID_SDK_ROOT=$HOME/android-sdk JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$ANDROID_HOME/ndk/26.1.10909125:$JAVA_HOME/bin:$PATH GRADLE_OPTS="-Xmx4g -Dorg.gradle.daemon=false -Dorg.gradle.parallel=true"`.
- **Five build fixes applied (in order encountered):**
  1. **Duplicate `externalNativeBuild` block** in `app/build.gradle` defaultConfig tried to set read-only `path` — removed the duplicate; `cmake { path; version }` lives at the android top-level only, `defaultConfig` keeps `cmake.arguments` + `ndk.abiFilters`.
  2. **Kotlin/Compose-Compiler mismatch** — RN 0.76.3's `gradle/libs.versions.toml` pins `kotlin=1.9.24`; the root `build.gradle` defaulted `ext.kotlinVersion` to `1.9.25`, which made `expo-modules-core` pick Compose Compiler 1.5.15 (needs Kotlin 1.9.25) while the actual Kotlin Gradle plugin applied was 1.9.24. Fix: change the `build.gradle` default to `1.9.24` (and remove any `android.kotlinVersion` gradle.properties override) so expo-modules-core's `versionsMap["1.9.24"]="1.5.14"` matches. Do **not** try to push Kotlin to 1.9.25 — RN 0.76.3 ships 1.9.24.
  3. **CMake `AUDIOSCOPE_SDK_ROOT` path off by one `../`** — `sdk/bindings/android/CMakeLists.txt` computed root as `../../..` (= repo root) but sources live under `sdk/` (e.g. `sdk/dsp/src/dsp.cpp`). Fixed to `../..` (= `sdk/`).
  4. **`expo.core.ExpoModulesPackage` not found in generated `PackageList.java`** — root cause: in this pnpm monorepo, expo's own `react-native.config.js` calls `expo-modules-autolinking`'s `findProjectRootSync()`, which can't locate `apps/vyzorMobile/android/settings.gradle` from the pnpm virtual store, so `isExpoModulesInstalledAndroid()` returns false and expo disables its `packageImportPath` override. The autolinking resolver then derives the import from expo's Android `namespace` (`expo.core`) instead of the class's real package (`expo.modules`), generating the invalid `import expo.core.ExpoModulesPackage;`. Fix: added `apps/vyzorMobile/react-native.config.js` that pins `dependencies.expo.platforms.android.packageImportPath = 'import expo.modules.ExpoModulesPackage;'` at the project level (this config IS loaded correctly via `projectConfig.dependencies.expo`).
  5. **Two C++ compile/link bugs in `sdk/bindings/android/`:** (a) `oboe_capture.cpp` used `stream_.reset_and_get_address()` — `std::shared_ptr` has no such method; oboe's `openStream(std::shared_ptr<oboe::AudioStream>&)` takes a reference, so pass `stream_` directly. (b) `jni_bridge.cpp` declared the binding factory/destroyer as plain `extern` (C++ linkage, mangled) but `oboe_capture.cpp` defined them as `extern "C"` (unmangled) → linker "undefined symbol". Fixed the declarations in `jni_bridge.cpp` to `extern "C"` to match.

## Mobile web→RN hooks/stores port (STEP 5 — hooks/stores aligned, DONE)
- **Scope:** Ported all web (`apps/vyzorWeb/src/`) hooks + stores to the RN app (`apps/vyzorMobile/app/`) so the mobile app mirrors the web's logic. 21 of 23 web hooks ported; 2 deliberately omitted (see below). `tsc --noEmit` clean (exit 0).
- **Foundation (new, `app/lib/`):** `device-id.ts` (RN device-id via AsyncStorage — replaces api-client's browser-bound localStorage getDeviceId), `api-config.ts` (env-based endpoints — replaces api-client's import.meta.env + window.location config), `apollo-client.ts` (Apollo client with HttpLink + WebSocketLink, X-Device-Id + Bearer headers — replaces api-client's browser-bound graphqlClient). The api-client gql operation files + domain types are pure TS (transport-agnostic) so the transport hooks import them unchanged.
- **Stores (`app/store/`):** `audio-store.ts` (RN-adapted — dropped MediaStream/AudioContext, kept devices/sampleRate/bufferSize/permissionState/systemInfo, persisted via AsyncStorage), `waveform-store.ts` (verbatim port), `ui-store.ts` (verbatim port, AsyncStorage persist). Barrel `store/index.ts` mirrors the web. Mobile-specific `scope-store.ts` + `settings-store.ts` kept (DSP results + DSP config — no direct web equivalent; web holds analyzer state in a module-level external store). Zustand `partialize` type errors fixed by using `createJSONStorage(() => AsyncStorage)` instead of a hand-rolled async storage adapter.
- **Transport hooks (verbatim ports — consume only @apollo/client + api-client gql):** `use-sessions`, `use-recordings`, `use-settings`, `use-session-settings`, `use-last-used-session`, `use-sessions-with-status`, `use-about`, `use-audio-settings`, `use-scope-capture` (globalThis.location → config URL), `use-waveform-stream` (globalThis.location → config URL; also exports useSubmitWaveform), `use-device-id` (getDeviceId via app/lib/device-id).
- **Native/capture hooks (RN-adapted — browser APIs swapped for RN/JNI DSP):** `use-audio-analyzer` (Oboe via Dsp.readSamples + C++ core measurements, module-level external store matching web's AudioAnalyzerState shape), `use-waveform-generator` (Dsp.generateWaveform), `use-media-devices` (Oboe default input, Platform info), `use-mobile` (useWindowDimensions), `use-theme` (Appearance API), `use-export` (expo-file-system + expo-sharing instead of <a download>), `use-streaming-csv-export` (fetch + Share), `use-chunked-playback` (SampleChunkService data layer intact, Web Audio playback stubbed with documented RN placeholder), `use-streaming-playback` (chunk fetch + state machine intact, AudioWorklet/WASM stubbed), `use-session-dialogs` (rename/delete mutation logic intact, dialog JSX dropped — components are a later phase).
- **Deliberately omitted:** `use-api-keys` (API keys not used in mobile — device-id is the only identity, per the port spec), `use-audio-context` (Web Audio AudioContext — RN capture runs through Oboe, no RN equivalent), `use-toast` (no RN toast system yet — use-session-dialogs logs to console instead).
- **Apollo wiring:** `app/_layout.tsx` now calls `ensureDeviceId()` then `buildApolloClient()` and wraps the app in `ApolloProvider` so the ported transport hooks (useQuery/useMutation) read from context. A loading shell renders until the client is built.
- **Screens updated:** `scope.tsx` now uses `useAudioAnalyzer` + `useScopeStore` (was use-mobile-scope, deleted). `settings.tsx` now reads `useAudioStore` + `useSettingsStore` directly (was use-mobile-settings, deleted). Both still minimal shells — full web-aligned components are the next phase.
- **Key constraint:** The `@/` path alias is web-only (Vite); RN uses relative imports. All ported hooks import via `../store`, `../lib/...`, `./...` (no `@/`). The api-client's `config.ts` and `graphql/client.ts` are browser-bound and MUST NOT be imported from RN — use `app/lib/api-config` + `app/lib/apollo-client` instead.

## Android audio input device enumeration (STEP 5 — C++ device enumeration, DONE)
- **Feature:** Enumerate the OS's connected audio input devices **by name** (builtin mic, wired headset, USB mics, Bluetooth), done **entirely in C++** — the user explicitly wanted to avoid Kotlin/Java-layer APIs where possible. The Kotlin side only passes the `ReactApplicationContext` through; no Java enumeration logic.
- **Two C++ sources merged** (new file `sdk/bindings/android/device_enumeration.{cpp,h}`):
  1. `android.media.AudioManager.getDevices(GET_DEVICES_INPUTS)` driven via JNI **from C++** (`FindClass`/`GetMethodID`/`CallObjectMethod` on the Context → getSystemService("audio") → getDevices(3)). Iterates `AudioDeviceInfo` for `getId()` (the Oboe-routable id), `getName()`, `getType()` (mapped to `builtin-mic`/`wired-headset`/`usb-device`/`usb-headset`/`bluetooth-sco`/`bluetooth-a2dp`/`dock`/`hdmi`/...), `getProductName()`, `isSource()`, `getSampleRates()`, `getChannelCounts()`. Every JNI call is followed by `ExceptionCheck`/`ExceptionClear` (a ROM-level exception would otherwise abort).
  2. Pure-C++ parse of `/proc/asound/cards` + `/proc/asound/cardN/id` + `/proc/asound/cardN/usbid` (and `/sys/class/sound/cardN/device/modalias` fallback) for the ALSA card short name + USB **vendor/product ids**, which the framework frequently does not expose (`productName` is often null on USB mics). Merged into the framework list by case-insensitive name match.
- **Return shape:** a JSON array string (parsed by the TS bridge) — `[{"id":"12","name":"...","type":"usb-device","productName":"...","isDefault":false,"sampleRates":[...],"channels":[...],"usbVendor":7224,"usbProduct":3164,"alsaCard":1}, ...]`. The first device is flagged `isDefault`.
- **Bridge surface (35 ↔ 35 ↔ 35, balanced):** `Java_com_audioscope_dsp_DspModule_nativeEnumerateInputDevices(env, klass, jobject context)` in jni_bridge.cpp ↔ `private external fun nativeEnumerateInputDevices(context: Context): String` in DspModule.kt ↔ `@ReactMethod fun enumerateInputDevices(promise)` resolving the JSON string. Added to CMakeLists.txt `audioscope_dsp` SHARED sources.
- **TS surface:** `AudioInputDevice` + `AudioInputDeviceKind` types + `Dsp.enumerateInputDevices(): Promise<AudioInputDevice[]>` (JSON.parse with `[]` fallback) in `app/lib/native-dsp-bridge.ts`. `use-media-devices.ts` now calls it instead of the hardcoded "Default input (Oboe)"; maps the rich list into the web `MediaDevice` store shape (`groupId = type`) and also exposes `inputDevices: AudioInputDevice[]` for richer UI; auto-selects the framework default; falls back to the Oboe default if the C++ query returns empty.
- **Verified:** `g++ -std=c++17 -fsyntax-only` clean on `device_enumeration.cpp` + `jni_bridge.cpp` (with a stub `jni.h` on the host — no NDK in this sandbox). `tsc --noEmit` clean. Braces balanced (jni_bridge.cpp 74/74, DspModule.kt 40/40). 35 JNI C++ exports ↔ 35 Kotlin `external fun` ↔ 35 `@ReactMethod` — `diff` empty. **Full Gradle Android build re-run after NDK install:** `./gradlew assembleDebug` → **BUILD SUCCESSFUL in 7m 47s** (exit 0); `:app:buildCMakeDebug` completed for all 4 ABIs (arm64-v8a, armeabi-v7a, x86, x86_64) with **no compile errors** — `device_enumeration.cpp` compiles cleanly with the real NDK 26.1.10909125. Fresh APK at `apps/vyzorMobile/android/app/build/outputs/apk/debug/app-debug.apk` (153 MB, Aug 11 18:36). `llvm-nm -D` on `libaudioscope_dsp.so` (both the build intermediate and the copy extracted from the APK) confirms `Java_com_audioscope_dsp_DspModule_nativeEnumerateInputDevices` is exported (`T`), alongside `JNI_OnLoad` — 35 `Java_..._native*` symbols total in the `.so`, matching the 35 Kotlin `external fun` / 35 `@ReactMethod`. The `.so` is packaged for all 4 ABIs inside the APK.
- **Caught a real C++ bug via the host syntax check:** `"...:" + (cond ? "true" : "false")` is pointer arithmetic on `const char*`, not string concat — fixed to `std::string(cond ? "true" : "false")`. The host `g++ -fsyntax-only` (with a stub jni.h) is a useful fast loop for catching such bugs before the slow NDK build.

## Linux/Windows native audio bindings (STEP 6 — §A.4, DONE)
- **Feature:** The ALSA + PulseAudio (Linux) and WASAPI (Windows) `AudioBinding` implementations — the desktop analog of Android's `oboe_capture.cpp`. All four platforms now implement the SAME `audioscope::bindings::AudioBinding` interface (enumerate/start/stop/read/is_capturing), so a native desktop client links the platform-appropriate target and reaches the shared C++ DSP core without a second capture path. The Rust server's `cpal` capture stays as-is (server-hosted `capture` mutation) — this step is for a desktop *client* app, per ARCHITECTURE_IMPLEMENTATION_SPEC §4 step 6.
- **New files:**
  - `sdk/bindings/linux/alsa_binding.cpp` — `AlsaCapture` via `snd_pcm_*`. `snd_device_name_hint` enumerates capture PCMs (IOID "Input"); `start_capture` opens the PCM, prefers `SND_PCM_FORMAT_FLOAT_LE`, falls back to `S16_LE` (normalized via `convert_samples_to_f32`), and reads on a background thread into a mutex-guarded `FloatRing`. Underruns recovered with `snd_pcm_recover`. Factory: `audioscope_linux_alsa_binding_create/destroy`.
  - `sdk/bindings/linux/pulse_binding.cpp` — `PulseCapture` via `pa_simple_*` (the simple blocking API). Exposes `default` + `auto_null.monitor` (headless loopback). `pa_sample_spec` requests `PA_SAMPLE_FLOAT32LE`, retries `PA_SAMPLE_S16LE`. Factory: `audioscope_linux_pulse_binding_create/destroy`.
  - `sdk/bindings/windows/wasapi_binding.cpp` — `WasapiCapture` via `IMMDeviceEnumerator` + `IAudioCaptureClient`. COM apartment initialized RAII-style (`CoInitializeEx`/`CoUninitialize`); `#ifdef _WIN32`-guarded so the file is a no-op translation unit on non-Windows hosts (CMake only adds the subdirectory under `if(WIN32)`). Coerces to mono float32 `WAVEFORMATEXTENSIBLE`, falls back to the device mix format, downmixes multi-channel to mono by averaging. Factory: `audioscope_windows_wasapi_binding_create/destroy`.
  - `sdk/bindings/linux/CMakeLists.txt` — `audioscope_bindings_linux_alsa` (links `pkg-config alsa`), `audioscope_bindings_linux_pulse` (links `pkg-config libpulse-simple` — **not** `libpulse`; `pa_simple_*` lives in the separate `-lpulse-simple`), and an `INTERFACE` aggregate `audioscope_bindings_linux` so a host links one name.
  - `sdk/bindings/windows/CMakeLists.txt` — `audioscope_bindings_windows` (links `ole32` for `CoInitialize`).
  - `sdk/tests/test_bindings.cpp` — GoogleTest smoke tests: factory returns non-null, `enumerate_devices()` non-empty with a flagged default, `read_samples` is a safe no-op before `start_capture`, `start_capture("__nonexistent_device__")` returns false (no stream opened — **no real hardware needed**), `stop_capture()` idempotent. Parametrized over alsa + pulse.
- **CMake wiring:** `sdk/CMakeLists.txt` now gates the binding subdirectories by host OS — `if(UNIX AND NOT APPLE) add_subdirectory(bindings/linux)` and `if(WIN32) add_subdirectory(bindings/windows)`. The Android binding is NOT built here (it's driven by the Gradle NDK build via `sdk/bindings/android/CMakeLists.txt` directly). New `linux-bindings` CMake preset (`AUDIOSCOPE_BUILD_BINDINGS=ON`) + matching build/test presets. `test_bindings` is only added to the test list when `AUDIOSCOPE_BUILD_BINDINGS AND UNIX AND NOT APPLE`.
- **Build + test (host):**
  ```
  cd sdk
  cmake --preset linux-bindings && cmake --build build/linux-bindings
  ctest --preset linux-bindings      # 65/65 (61 core + 4 bindings)
  cmake --preset linux && cmake --build build/linux && ctest --preset linux  # 61/61 regression — default preset unaffected
  ```
  Host deps (per SETUP_GUIDE Step 2): `cmake ninja-build pkg-config libasound2-dev libpulse-dev liblz4-dev` + `g++` (C++17). `pkg-config` confirms `alsa` (1.2.14) + `libpulse-simple` (17.0) + `liblz4` (1.10.0).
- **WASAPI host syntax check (no Windows host available):** `g++ -std=c++17 -fsyntax-only -D_WIN32` clean on `wasapi_binding.cpp` using faithful Windows-SDK stub headers (`atlbase.h`/`mmdeviceapi.h`/`audioclient.h`/`objbase.h` — `CComPtr` with `operator bool`, `IMMDevice : IUnknown`, `IAudioClient::GetService`, `WAVEFORMATEXTENSIBLE.Samples` union, `FAILED`/`SUCCEEDED`/`CLSCTX_ALL`/`COINIT_MULTITHREADED`/`WAVE_FORMAT_IEEE_FLOAT`). The real Windows build (MSVC + the actual Windows SDK) is the authoritative check; this host loop mirrors the Android `g++ -fsyntax-only` + stub-`jni.h` pattern for catching bugs before the slow platform build.
- **Caught a real bug via the host syntax check:** `std::string(desc, nl)` (pointer + pointer) is not a valid `std::string` ctor — fixed to `std::string(desc, static_cast<std::size_t>(nl - desc))` (pointer + length). Same class of bug as the Step 5 `const char*` arithmetic.
- **`pkg-config libpulse` vs `libpulse-simple`:** the PulseAudio *simple* API (`pa_simple_new`/`pa_simple_read`/`pa_simple_free`) lives in `libpulse-simple`, which `pkg-config --libs libpulse` does **not** include (it returns only `-lpulse`). Link against the `libpulse-simple` module — it transitively pulls `-lpulse`. This bit the first link (`undefined reference to pa_simple_*`).

## ESP32 firmware + bare-USB host binding (STEP 9 — §A.6, DONE)
- **Design decision (overrides ARCHITECTURE.md's earlier UAC plan):** the ESP32 presents a **custom vendor-class USB device**, NOT a USB Audio Class device. The host talks to the processor **directly via libusb**, bypassing the OS audio stack (ALSA/WASAPI/CoreAudio/Oboe). User explicitly wanted "bare" / no usual audio paths. Tradeoff recorded: loses plug-and-play with arbitrary DAWs/recorders (needs the audioscope host or any libusb client); gains direct processor control, a real control surface (gain/rate/channels/timebase/trigger/vscale/coupling) UAC lacks, lower latency, no OS audio processing. Rationale + wire-protocol spec in `docs/ESP32_USB_PROTOCOL.md`.
- **It's a real oscilloscope, not just an audio capture:** raw ADC acquisition at the device's actual bandwidth (host queries `GET_BANDWIDTH` → real PCM1802 datasheet limits: 96 kHz max rate, 77 kHz analog BW, 24-bit, 3200 mV p-p range; effective BW = min(adc_max_rate/2, analog_bw_hz)); PWM/signal generation output (`generator.c`, LEDC hardware PWM up to 5 MHz for square/DC + sine/tri/saw via DAC); configurable scope settings (`SET_TIMEBASE`, `SET_TRIGGER`, `SET_VSCALE`, `SET_COUPLING`).
- **New files — firmware (`sdk/firmware/esp32/`, ESP-IDF v5.4 + vendored TinyUSB):**
  - `CMakeLists.txt` (top-level ESP-IDF project; `EXTRA_COMPONENT_DIRS` points at the vendored `components/tinyusb`; `include($ENV{IDF_PATH}/.../project.cmake OPTIONAL)` so it browses in a plain CMake editor without IDF), `partitions.csv`, `sdkconfig.defaults` (USB OTG + I2S + PSRAM; NO `CONFIG_ESP_TINYUSB` kconfig — that's the managed-component wrapper we don't use), `idf_component.yml` (no deps — TinyUSB vendored locally).
  - `components/tinyusb/` — vendored real TinyUSB (upstream `hathach/tinyusb` master, Synopsys DWC2 DCD for the S3's native USB OTG). `tusb_config.h` enables only the vendor class (CDC/MSC/HID/AUDIO/MIDI all 0). `CMakeLists.txt` compiles `tusb.c`/`tusb_fifo.c`/`vendor_device.c`/`dcd_dwc2.c`/`dwc2_common.c` and defines `CFG_TUSB_MCU=OPT_MCU_ESP32S3`, `CFG_TUSB_OS=OPT_OS_FREERTOS`, `CFG_TUSB_OS_INC_PATH=freertos/` (PUBLIC — ESP-IDF's FreeRTOS headers live under `include/freertos/`).
  - `main/main.c` — `app_main`: init USB → codec → ring → control/stream/generator tasks.
  - `main/usb_descriptors.c` — TinyUSB device + config descriptors (**vendor class**, bulk IN 0x81 + OUT 0x01, `TUD_VENDOR_DESCRIPTOR`; NOT UAC). VID 0x1209 / PID 0xA500 (pid.codes community; overridable via `-D`).
  - `main/usb_device.c/.h` — TinyUSB vendor-class callbacks (`tud_vendor_control_xfer_cb` → `tud_control_status`; `tud_vendor_rx_cb`/`tx_cb` with `uint32_t` lengths; `usb_device_send_response` → `tud_vendor_write`).
  - `main/codec.c/.h` — I2S codec driver via ESP-IDF `driver/i2s_std.h` (`i2s_channel_init_std_mode`); PCM1802 default (real 24-bit ADC, `codec_bandwidth()` returns datasheet specs), UDA1334A/WM8731 clean swap-point. On-chip ADC explicitly avoided (too low-rate/noisy).
  - `main/generator.c/.h` — waveform generator (LEDC hardware PWM: square/DC up to `AS_USB_GEN_MAX_FREQ_HZ`=5 MHz, 12-bit duty; sine/tri/saw via DAC). `generator_start/stop`.
  - `main/ring_buffer.c/.h` — FreeRTOS ring between DMA-read task and USB bulk-IN task.
  - `main/stream_task.c/.h` — codec → ring → bulk-IN pump (frames the stream with `as_usb_stream_header`).
  - `main/control_task.c/.h` — host command dispatcher (GET_INFO/GET_BANDWIDTH/START/STOP/SET_RATE/SET_GAIN/SET_CHANNELS/SET_TIMEBASE/SET_TRIGGER/SET_VSCALE/SET_COUPLING/GEN_*; owns the streaming flag).
  - `main/board_config.h` — pin map (I2S BCLK=4/LRCK=5/DOUT=6, PCM1802 wiring) + USB endpoint config + `AUDIOSCOPE_CODEC_ID`.
  - `README.md` — wiring, codec choice, analog front-end, build/flash.
- **New files — host binding (`sdk/bindings/usb/`, libusb-1.0):**
  - `usb_protocol.h` / `usb_protocol.c` — **the single source of truth** for the wire format (magic, command opcodes, 16-byte cmd/rsp headers, 64-byte `as_usb_device_info`, stream header) + CRC-16/CCITT reference impl. Pure C99 — compiles under ESP-IDF AND host g++/clang. `AS_USB_STATIC_ASSERT` guards freeze struct sizes.
  - `usb_binding.h` / `usb_binding.cpp` — `UsbCapture : AudioBinding`. `enumerate_devices()` → libusb device list filtered by VID/PID + `GET_INFO`. `start_capture()` → open, claim interface, `START_STREAM` control packet, reader thread (async bulk-IN) → `FloatRing` → `convert_samples_to_f32` (native S16/S24/S32/F32 → canonical float32; multi-channel downmixed to mono by averaging). `stop_capture()`, `read_samples()`, `is_capturing()`. Factory: `audioscope_usb_binding_create/destroy` (same shape as the other bindings).
  - `CMakeLists.txt` — `audioscope_usb_protocol` (static) + `audioscope_bindings_usb` (static). **Cross-platform** (libusb works on Linux/Win/macOS) — gated on `pkg-config libusb-1.0`, NOT on host OS like ALSA/WASAPI.
  - `README.md` — host libusb setup, udev rule, the bypass-the-OS-audio-stack rationale.
- **New files — docs + tests:**
  - `docs/ESP32_USB_PROTOCOL.md` — authoritative wire-protocol spec (endpoints, packet layouts, opcodes, status codes, sample formats, GET_INFO response, CRC).
  - `sdk/tests/test_usb_binding.cpp` — protocol ABI assertions (`sizeof(as_usb_cmd_header)==16`, `rsp==16`, `device_info==64`, `stream==4`), magic constants, CRC-16 known vector (`crc16("123456789")==0x29B1`), + no-hardware binding smoke (factory, enumerate-safe-empty, bogus `start_capture`→false, idempotent `stop`, read-before-start no-op).
- **CMake wiring:** `sdk/CMakeLists.txt` adds `bindings/usb` when `pkg-config libusb-1.0` found (inside `AUDIOSCOPE_BUILD_BINDINGS`); `sdk/tests/CMakeLists.txt` adds `test_usb_binding` linking `audioscope_bindings_usb` under the same gate. Firmware is NOT in the host preset (separate `idf.py` toolchain) — its `CMakeLists.txt` `OPTIONAL`-includes `project.cmake` so it browses without ESP-IDF.
- **Build + test (host, real):**
  ```
  sudo apt-get install libusb-1.0-0-dev liblz4-dev libpulse-dev   # libusb 1.0.28
  cd sdk
  cmake --preset linux && cmake --build build/linux
  ctest --preset linux     # 78/78 (61 core + 4 linux + 13 usb)
  ```
  (cmake 4.4.2 + ninja via pip; `linux` preset now links usb tests when libusb present.)
- **Verification matrix — ALL real builds (this sandbox installs real toolchains):**
  - `usb_protocol.c` — pure C99, compiles under both ESP-IDF and host g++.
  - `usb_binding.cpp` + `test_usb_binding.cpp` — **real build + link + test** (libusb via apt).
  - **firmware — REAL `idf.py build`** (ESP-IDF v5.4 installed at `/opt/esp-idf`, IDF_TOOLS_PATH=`/home/openhands/.espressif`, Python 3.13 env, xtensa-esp32s3-elf-gcc). TinyUSB is vendored as a local component (`sdk/firmware/esp32/components/tinyusb/`, upstream `hathach/tinyusb` master, Synopsys DWC2 DCD for the S3's native USB OTG) registered via `EXTRA_COMPONENT_DIRS` — NOT the IDF Component Registry (registry was inaccessible offline). Build output: `build/audioscope_esp32.bin` (246 KB) + `build/bootloader/bootloader.bin` (21 KB) + partition table. `Project build complete`. Source env then `idf.py set-target esp32s3 && idf.py build`.
- **Real ESP-IDF build caught real bugs the host syntax-check loop couldn't (the point of building with the real toolchain):**
  1. `i2s_channel_init_std_rx_channel` → `i2s_channel_init_std_mode` (ESP-IDF v5.4 API rename).
  2. `main/CMakeLists.txt` INCLUDE_DIRS depth wrong (`../../bindings/usb` → `../../../bindings/usb`).
  3. `codec.c` missing `#include "generator.h"` for `AS_USB_*` constants.
  4. TinyUSB master's `osal_freertos.h` uses bare `#include "FreeRTOS.h"`; ESP-IDF's FreeRTOS headers live under `include/freertos/`, so the component must define `CFG_TUSB_OS_INC_PATH=freertos/` (PUBLIC) for the osal includes to resolve.
  5. Real TinyUSB vendor-class API differs from the stubbed guesses: callbacks are `tud_vendor_rx_cb(itf, buf, uint32_t)` / `tud_vendor_tx_cb(itf, uint32_t)` (NOT uint16_t), descriptor macros are `TUD_CONFIG_DESCRIPTOR`/`TUD_VENDOR_DESCRIPTOR` (NOT `TUD_CONFIG_DESC`/`TUD_VENDOR_DESC`), and `tud_vendor_control_xfer` doesn't exist (use `tud_control_status`/`tud_control_xfer`).
  6. `control_task.c` used `AUDIOSCOPE_CODEC_PCM1802` (undefined) — real constant is `AS_USB_CODEC_PCM1802`.
  7. `generator.c` `%u` with `uint32_t` (Xtensa ABI = `long unsigned`) → `-Werror=format=`; cast to `(unsigned long)` + `%lu`.
- **Hardware TODO (not blocking):** `idf.py flash` + real PCM1802 oscilloscope round-trip on an ESP32-S3 DevKit. The codec layer has a clean swap-point (`AUDIOSCOPE_CODEC_ID` in `board_config.h`, defaults to PCM1802). The firmware binary is real and flashable; only on-device validation remains.
